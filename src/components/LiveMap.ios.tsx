// iOS uses the same MapLibre live map as Android. One visual language, one rendering stack.
// Platform-specific Apple Maps previously made an active Travel look like a different product.
export { default, LiveMap } from './LiveMap';
