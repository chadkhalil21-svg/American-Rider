{
  "name": "american-rider-server",
  "version": "0.1.0",
  "private": true,
  "description": "American Rider payment + backend server. Holds the Stripe secret key so the app never has to.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "eslint .",
    "test": "for f in *.test.js; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "firebase-admin": "^14.2.0",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5"
  }
}